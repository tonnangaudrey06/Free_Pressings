CREATE TABLE Contenu 
(
    nom_pressing
    etat                         varchar2(255),
   nom_offre varchar2(255),
   quantité  number(10),
   Pressingid_pressing,
   PRIMARY KEY (id_contenu)
  
);
